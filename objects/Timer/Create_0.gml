// Create Event of obj_timer object
	depth = -98;

   timer_duration_seconds = 300; // Timer duration in seconds
   timer_current_seconds = timer_duration_seconds; // Initialize the current timer value in seconds
   timer_active = false; // Set the timer to active
